# 🚀 Быстрый старт

## Миграция завершена!
Весь бэкенд Express.js теперь работает на Next.js API Routes.

## Установка

```bash
# Установить зависимости (уже выполнено)
npm install
```

## Настройка

Создайте файл `.env.local` в папке `frontend`:

```env
# База данных MongoDB
DATABASE_LOGIN=your_login
DATABASE_PASS=your_password
DATABASE_URL=localhost
DATABASE_PORT=27017

# Секретные ключи
SECRET_KEY=your_jwt_secret
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000
```

## Запуск

```bash
# Режим разработки
npm run dev

# Продакшн
npm run build
npm start
```

Приложение запустится на `http://localhost:3000`

## API Endpoints

Все API теперь доступны через `/api/*`:

- `/api/users` - Управление пользователями
- `/api/login` - Авторизация
- `/api/objects` - Объекты недвижимости
- `/api/analytics` - Аналитика
- `/api/bookings` - Бронирования
- `/api/bysuness` - Загруженность
- `/api/options` - Настройки
- `/api/sync` - Синхронизация с Beds24

## Что изменилось?

- ✅ Больше не нужен отдельный бэкенд-сервер
- ✅ Все работает на одном порту
- ✅ Переменная `NEXT_PUBLIC_API_URL` больше не используется
- ✅ Улучшена производительность

## Проблемы?

Смотрите подробное руководство в `../MIGRATION_GUIDE.md`

